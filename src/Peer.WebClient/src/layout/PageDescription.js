import React from 'react'

const PageDescription = ({ description }) => {
    return (
        <p className='h-8 text-base text-gray-500 text-center'>{description}</p>
    )
}

export default PageDescription